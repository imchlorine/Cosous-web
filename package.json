{
  "name": "cosous-web",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing and support site for Cosous, built with Eleventy.",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
